services:
  app:
    build:
      context: .
    ports:
      - "3000:3000"
    environment:
      NODE_ENV: production
      PORT: 3000
      DATABASE_PATH: /app/data/users.sqlite
      CLIENT_DIST_DIR: /app/client/dist
    volumes:
      - ./data:/app/data
